---
description: Inspect Ghost Trust Runtime — durable runs, traces, evals, approvals.
---

$ARGUMENTS is the subaction (status|runs|trace|eval|eval-cases) plus optional flags. Call `ghost_trust` with `action` set accordingly and forward remaining flags via `args`.
