---
description: Run Ghost's deterministic PR/diff review checks (secrets, destructive cmds, missing tests, beta drift).
---

Call `ghost_review_pr` with `{"args": $ARGUMENTS or []}`. Summarise the findings by category.
